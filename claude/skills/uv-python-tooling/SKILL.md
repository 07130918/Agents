---
name: uv-python-tooling
description: uv (Astral) を使った Python 実行・依存管理のベストプラクティス。`uv sync` / `uv run` / `uv pip` の使い分け、pyproject.toml と uv.lock、仮想環境、❌ 禁止コマンド (素の python3/pip)。Python プロジェクトのコマンド実行・依存追加・テスト・lock ファイル管理時に参照する。
---

# uv-python-tooling

この skill の詳細手順は `~/.agents/references/uv-python-tooling.md` に集約しています。

この skill が発火したら、作業前に必ず上記の参照ファイルを読み、実行中の CLI に対応する手順・チェックリスト・注意事項に従ってください。
