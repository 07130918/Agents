---
name: gcp-cloud-run-ops
description: GCP Cloud Run、Cloud Build、Cloud SQL、Secret Manager、Artifact Registry、Terraform、Cloud Run logs、開発/本番環境への反映を調査・運用するときに使う。ユーザーが「GCPに反映」「Cloud Runのログ」「Secretを更新」「Cloud Buildが失敗」と依頼したときに使う。localhost や Docker Compose は `local-dev-ops` を使う。
allowed-tools: Read Write Edit Glob Grep Bash(find *) Bash(wc *) Bash(mkdir *)
---

# gcp-cloud-run-ops

この skill の詳細手順は `~/.agents/references/gcp-cloud-run-ops.md` に集約しています。

この skill が発火したら、作業前に必ず上記の参照ファイルを読み、実行中の CLI に対応する手順・チェックリスト・注意事項に従ってください。
