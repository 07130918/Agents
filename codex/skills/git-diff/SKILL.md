---
name: git-diff
description: |
  現在のブランチとベースブランチ(develop優先、なければmain)の差分を表示し、日本語で要約する。
  Use when: user asks to inspect branch diff, summarize changes, or run /git-diff.
  Accepts args: none.
---

# git-diff

この skill の詳細手順は `~/.agents/references/git-diff.md` に集約しています。

この skill が発火したら、作業前に必ず上記の参照ファイルを読み、実行中の CLI に対応する手順・チェックリスト・注意事項に従ってください。
